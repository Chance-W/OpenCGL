<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.opencgl</groupId>
    <artifactId>OpenCGL_New</artifactId>
    <version>1.0-SNAPSHOT</version>


    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <software-version>2.2.3</software-version>
        <maven.build.timestamp.format>yyyyMMdd</maven.build.timestamp.format>
        <build.timestamp>${maven.build.timestamp}</build.timestamp>
        <junit.version>5.10.2</junit.version>
        <mockito.version>5.14.2</mockito.version>
    </properties>
    <profiles>
        <profile>
            <id>windows</id>
            <activation>
                <os>
                    <family>windows</family>
                </os>
            </activation>
            <properties>
                <icon-name>icon.ico</icon-name>
                <package-type>exe</package-type>
                <package-args>--win-per-user-install --win-dir-chooser --win-menu --win-shortcut --win-shortcut-prompt
                    --install-dir OpenCGL --win-upgrade-uuid 079cc66a-4f5e-45df-8198-3959f3936d6f
                </package-args>
            </properties>
        </profile>
        <profile>
            <id>mac</id>
            <activation>
                <os>
                    <family>mac</family>
                </os>
            </activation>
            <properties>
                <icon-name>mac-icon.icns</icon-name>
                <package-type>dmg</package-type>
                <package-args/>
            </properties>
        </profile>
        <profile>
            <id>linux</id>
            <activation>
                <!--
                按发行版装打包工具
                Fedora/RHEL/CentOS（打 rpm）：
                sudo dnf install rpm-build（或等价命令）
                Debian/Ubuntu（建议打 deb）：
                sudo apt install fakeroot
                然后执行：
                mvn -Dpackage-type=deb package
                （会覆盖 profile 里的 package-type，生成 .deb）
                -->
                <!-- unix = Linux；mac 已单独用 family=mac 激活，不会冲突 -->
                <os>
                    <family>unix</family>
                </os>
            </activation>
            <properties>
                <icon-name>logo.png</icon-name>
                <!-- 默认 rpm；Debian/Ubuntu 建议: mvn -Dpackage-type=deb package -->
                <package-type>rpm</package-type>
                <package-args>--linux-shortcut --linux-app-category Utility --linux-app-release 1</package-args>
            </properties>
        </profile>
    </profiles>


    <dependencies>
        <dependency>
            <groupId>com.opencgl</groupId>
            <artifactId>com.opencgl.api</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.opencgl</groupId>
            <artifactId>com.opencgl.base</artifactId>
            <version>1.0.0</version>
        </dependency>
        <!-- 测试 -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <version>${mockito.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <resources>
            <!-- 开启对 application.properties 的资源过滤，以注入 pom.xml 中的版本号 -->
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>com/opencgl/config/application.properties</include>
                </includes>
            </resource>
            <!-- 排除 application.properties 被普通复制，防止覆盖 -->
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>com/opencgl/config/application.properties</exclude>
                </excludes>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.1</version>
                <configuration>
                    <release>${maven.compiler.release}</release>
                    <optimize>true</optimize>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.4</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
                <version>3.6.1</version>
                <executions>
                    <execution>
                        <id>require-java-21</id>
                        <goals>
                            <goal>enforce</goal>
                        </goals>
                        <configuration>
                            <rules>
                                <requireJavaVersion>
                                    <version>[21,22)</version>
                                    <message>OpenCGL must be built with Azul Zulu JDK 21. Check JAVA_HOME and java -version.</message>
                                </requireJavaVersion>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.1</version>
                <configuration>
                    <finalName>OpenCGL</finalName>
                    <archive>
                        <!--生成的jar中，不要包含pom.xml和pom.properties这两个文件 -->
                        <addMavenDescriptor>false</addMavenDescriptor>
                        <manifest>
                            <!--运行jar包时运行的主类，要求类全名-->
                            <mainClass>com.opencgl.OpenCglStartApplication</mainClass>

                            <!-- 是否指定项目classpath下的依赖 -->
                            <addClasspath>true</addClasspath>
                            <!-- 指定依赖的时候声明前缀 -->
                            <classpathPrefix>./lib/</classpathPrefix>
                            <useUniqueVersions>false</useUniqueVersions>
                        </manifest>
                    </archive>
                    <excludes>
                        <exclude>${project.basedir}/conf/</exclude>
                    </excludes>
                </configuration>
            </plugin>
            <!--把当前项目所有的依赖打包到target目录下的lib文件夹下-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>2.8</version>
                <executions>
                    <execution>
                        <id>copy</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/lib</outputDirectory>
                            <!-- 发布目录只包含 compile/runtime 依赖，不携带 JUnit、Mockito 等测试库。 -->
                            <includeScope>runtime</includeScope>
                            <!--已存在的Release版本不重复copy-->
                            <overWriteReleases>false</overWriteReleases>
                            <!--已存在的SnapShot版本不重复copy-->
                            <overWriteSnapshots>false</overWriteSnapshots>
                            <!--不存在或者有更新版本的依赖才copy-->
                            <overWriteIfNewer>false</overWriteIfNewer>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!--含依赖，但不包含jre的完整jar包，需要结合jdk使用-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <version>2.5.5</version>
                <configuration>
                    <finalName>OpenCGL-Single</finalName>
                    <!-- 保留 OpenCGL.jar 作为原生打包入口，胖 JAR 使用独立后缀，禁止覆盖主构件。 -->
                    <appendAssemblyId>true</appendAssemblyId>
                    <encoding>utf-8</encoding>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <mainClass>com.opencgl.OpenCglStartApplication</mainClass>
                        </manifest>
                    </archive>
                    <descriptorRefs>
                        <descriptorRef>jar-with-dependencies</descriptorRef>
                    </descriptorRefs>
                </configuration>
                <executions>
                    <execution>
                        <id>make-assembly</id>
                        <phase>package</phase>
                        <goals>
                            <goal>single</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
