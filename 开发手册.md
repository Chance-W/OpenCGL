# OpenCGL 插件开发指南

欢迎加入 OpenCGL 插件开发！OpenCGL 提供了一个灵活的插件化平台，允许开发者使用 JavaFX (推荐)、Swing 或 Web 技术构建功能丰富的工具。

---

## 目录

1. [架构概览](#架构概览)
2. [环境准备](#环境准备)
3. [创建第一个插件](#创建第一个插件)
4. [核心 API 说明](#核心-api-说明)
5. [插件国际化](#插件国际化)
6. [UI 开发指南](#ui-开发指南)
7. [生命周期管理](#生命周期管理)
8. [依赖冲突解决 (Shade 阴影技术)](#依赖冲突解决-shade-阴影技术)
9. [构建与调试](#构建与调试)
10. [最佳实践](#最佳实践)

---

## 架构概览

OpenCGL 采用 SPI (Service Provider Interface) 机制加载插件。每个插件是一个独立的 JAR 包，运行在独立的 ClassLoader 中（由 PF4J 或通过 URLClassLoader 加载），以实现隔离。

**核心模块：**
*   **OpenCGL_New**: 主程序容器，负责加载插件、管理 UI 框架。主程序**直接依赖** `com.opencgl.base`（编译并打包进主程序），运行时由主程序提供 Base，插件与之共用同一份，因此主题、语言、LoadingMask 等公共组件由主程序统一控制。
*   **PluginApiModule**: 定义了插件接口 `PluginUI`。
*   **OpenCGL-Base**: 公共工具类、依赖库（Hutool, OkHttp, FastJson 等）和基础 UI 组件。插件侧**不**在各自模块里写死 Base 依赖，而是通过**父 POM** `com.opencgl.plugin` 的 **Maven profile** 统一控制：默认 **plugin-mode** 下以 `provided` 注入（运行时用主程序的 Base），**stand-alone-mode** 下参与打包便于单独运行。

**依赖关系小结**：主程序依赖 Base 是合理且必要的（主程序是 Base 的提供方）；插件通过 parent 的 profile 获得公共包，避免每个插件重复声明且与主程序版本一致。若主程序与插件工程分仓库维护，建议通过统一父 POM 或 BOM 管理 `com.opencgl.base` 的版本，避免两处升级不同步。

**插件依赖原则（主程序约定）**：
*   **OpenCGL-Base** 维护基础公共包（JavaFX、MaterialFX、Hutool、日志等），插件通过**父 POM 的 profile** 依赖 Base（plugin-mode 下 `provided`，stand-alone 下 `compile`）。
*   **若插件需要 Base 中不存在的库**（如 commonmark、kafka-clients、某款 MQ 客户端等），须在**该插件模块内自行声明依赖**，并自行负责打包：主程序按**单 JAR** 加载插件（URLClassLoader 只加载一个 JAR），因此这类“额外依赖”必须打进插件 JAR（例如使用 `maven-shade-plugin`），否则运行时会报 `NoClassDefFoundError`。

---

## 环境准备

*   **JDK**: 21 或更高版本
*   **Maven**: 3.6+
*   **IDE**: IntelliJ IDEA (推荐)

---

## 创建第一个插件

### 1. 创建 Maven 项目

创建一个新的 Maven 模块，在 `pom.xml` 中添加核心依赖。建议将 `OpenCGL-Base` 作为父工程或引入其依赖。

```xml
<dependencies>
    <!-- 必须：插件 API 接口 -->
    <dependency>
        <groupId>com.opencgl</groupId>
        <artifactId>com.opencgl.api</artifactId>
        <version>1.0.0</version>
        <scope>provided</scope>
    </dependency>
    
    <!-- 推荐：基础库 (提供 JavaFX, MaterialFX, Hutool 等) -->
    <dependency>
        <groupId>com.opencgl</groupId>
        <artifactId>com.opencgl.base</artifactId>
        <version>1.0.0</version>
        <scope>provided</scope>
    </dependency>
</dependencies>
```

> **注意**: 依赖的 scope 应设为 `provided`，因为主程序运行时已包含这些类库，避免插件包体积膨胀及类冲突。

若在**插件工程父 POM**（如 `com.opencgl.plugin`）下开发，父 POM 已通过 **profile** 区分两种模式，默认使用 **plugin-mode**，上述依赖会以 `provided` 形式注入，运行时统一使用主程序提供的 OpenCGL-Base；需要单独运行插件时可切换为 **stand-alone-mode**，依赖会参与打包。

### 2. 实现 PluginUI 接口

创建类实现 `com.opencgl.api.PluginUI`：

```java
package com.myplugin;

import com.opencgl.api.PluginUI;
import javafx.fxml.FXMLLoader;
import javafx.scene.Parent;
import java.io.IOException;
import java.net.URL;

public class MyPluginUI implements PluginUI {

    @Override
    public String name() {
        return "我的测试插件"; // 侧边栏显示的名称
    }

    @Override
    public String directoryName() {
        return "开发工具"; // 侧边栏分类名称
    }

    @Override
    public UIType type() {
        return UIType.JAVAFX; // 推荐使用 JAVAFX
    }
    
    @Override
    public URL iconPath() {
        // 返回图标 URL，例如：
        return this.getClass().getClassLoader().getResource("icon.png");
    }

    @Override
    public Object createView() {
        // 创建 UI 界面
        try {
            FXMLLoader loader = new FXMLLoader();
            // 关键：必须设置 ClassLoader，否则无法找到 FXML 中的 Controller
            loader.setClassLoader(this.getClass().getClassLoader());
            loader.setLocation(this.getClass().getResource("/views/MyView.fxml"));
            return loader.load();
        } catch (IOException e) {
            throw new RuntimeException("UI 加载失败", e);
        }
    }

    @Override
    public void dispose() {
        // 插件关闭时的资源清理
        System.out.println("插件已卸载");
    }
}
```

### 3. 配置 SPI

在 `src/main/resources/META-INF/services/` 目录下创建文件：
文件名：`com.opencgl.api.PluginUI`
内容：
```text
com.myplugin.MyPluginUI
```

---

## 核心 API 说明

### PluginUI 接口

| 方法 | 说明 |
| :--- | :--- |
| `name()` | 插件在侧边栏显示的名称（必须唯一）。 |
| `directoryName()` | 插件所属的分类组（如 "网络工具"），相同分类会自动归组。 |
| `iconPath()` | 侧边栏图标资源。支持图片路径。 |
| `type()` | UI 类型枚举：`JAVAFX`, `SWING`, `WEB`。 |
| `createView()` | 初始化并返回 UI 对象。仅在用户点击打开插件时调用一次。 |
| `dispose()` | 应用关闭或插件卸载时调用，用于释放线程池、网络连接等资源。 |

### 图标与分类说明

*   **iconPath()**：主程序会通过 `URL.openStream()` 读取图标字节后再构造 `Image`，避免直接使用 `jar:file:...` URL 导致的 ZipException。插件只需返回资源 URL（如 `getClass().getResource("/icon.png")`），可为 `null` 使用主程序默认图标。
*   **directoryName()**：用于侧栏分类与排序。若主程序在 `opencgl_zh_CN.properties` / `opencgl_en.properties` 中配置了 `opencgl.category.<分类名>`，侧栏会随主程序语言切换显示对应译文；否则使用插件返回的字符串。建议与主程序已有分类 key 一致，或在主程序中补充对应 key。

### 内置工具库 (OpenCGL-Base)

插件可以直接使用 `OpenCGL-Base` 暴露的库，无需重复引入：
*   **Hutool**: `cn.hutool.*` (丰富的 Java 工具类)
*   **OkHttp3**: 网络请求
*   **FastJson**: JSON 处理
*   **MaterialFX**: `io.github.palexdev.materialfx.*` (现代化 UI 组件)
*   **Log4j / SLF4j**: 日志记录

---

## 插件国际化

插件可选择以下方式支持多语言，与主程序语言切换一致。

### 方式一：依赖 OpenCGL-Base（推荐）

使用 `com.opencgl.base.utils.i18n.BaseI18N` 与项目内的 `I18nResolver`，在资源目录放置 `YourModule_zh_CN.properties` / `YourModule_en.properties`，通过 `I18N.get("key")`、`I18N.getBinding("key")` 获取文案与绑定。在**插件模式**下，OpenCGL-Base 由主程序提供，全局 Locale、主题等与主程序一致，主程序切换语言时插件取文案即可得到新语言。

### 方式二：自定义实现（不依赖 Base）

若插件不希望依赖 OpenCGL-Base，可自行使用 JDK `ResourceBundle` + `MessageFormat` 实现：

*   Bundle 基名与资源路径与主程序约定一致（如 `com.you.plugin.i18n.YourPlugin`）。
*   `getLocale()` 可使用 `Locale.getDefault()`，主程序切换语言时会设置默认 Locale。
*   仅需在 `name()`、`directoryName()` 等需要展示的接口中调用 `I18N.get("key")` 即可。

### 分类名的多语言

侧栏分类显示名由主程序统一解析：若存在 `opencgl.category.<directoryName 返回值>`，则使用主程序配置的中/英文；否则直接显示插件返回的字符串。插件只需保证 `directoryName()` 返回值稳定（如固定返回「开发工具」或英文 key），主程序侧配置对应翻译即可。

---

## UI 开发指南

### 推荐：JavaFX + FXML
1.  使用 SceneBuilder 设计界面。
2.  在 FXML 中绑定 Controller。
3.  使用 MaterialFX 控件（如 `MFXButton`, `MFXTextField`）以获得与主程序一致的体验。

### 样式适配
主程序会自动注入 CSS。若需自定义样式，请确保 CSS 文件路径正确。
支持深色模式适配：插件应尽量使用 CSS 变量或监听主题变化事件（如果实现了 `ThemeAware` 接口）。

### createView() 与 UI 线程
主程序在 JavaFX 应用线程中调用 `createView()`。若此处执行耗时较长（如加载大 FXML、初始化 WebView），会短暂占用 FX 线程，LoadingMask 的转圈动画可能在该段时间内不更新。建议在 `createView()` 中只做必要、轻量的 UI 构建，重量级加载或网络请求放到后台线程，再用 `Platform.runLater` 更新界面。

---

## 生命周期管理

### 初始化
`createView()` 是懒加载的。只有当用户第一次点击插件图标时才会调用。建议在此处初始化重量级资源。

### 销毁 (Disposal)
这是**极其重要**的一步。主程序关闭时会调用 `dispose()`。
务必在此方法中：
1.  关闭所有 `ExecutorService` / 线程池。
2.  关闭定时任务 `ScheduledExecutorService`。
3.  关闭文件流或网络连接 (如 `OkHttpClient`, `ZkClient`)。

**示例：**
```java
@Override
public void dispose() {
    if (myHttpClient != null) {
        myHttpClient.dispatcher().executorService().shutdown();
    }
    if (myScheduledFuture != null) {
        myScheduledFuture.cancel(true);
    }
}
```

## 依赖冲突解决 (Shade 阴影技术)

### 为什么会发生冲突？
OpenCGL 使用标准的 `URLClassLoader` (采用 Parent-First 双亲委派模型) 加载插件。当插件引入了庞大的第三方框架（如 Apache Dubbo、ZooKeeper、Netty、Spring 等），且这些框架版本与主程序或其他插件底层存在重叠或不兼容时，极易引发 `NoSuchMethodError` 或 `ClassNotFoundException`。

如果试图通过强行重写 `CustomClassLoader` 改为 **子优先 (Child-First)** 隔离，往往会引发更加致命的架构级问题：
1. **SPI (ServiceLoader) 强转报错**：核心契约接口 `PluginUI` 因分别存在于两个 ClassLoader 空间内，被视为两个完全不同的类，导致注入主程序时抛出 `ClassCastException`。
2. **多线程/异步环境状态穿透**：诸如 Dubbo、Zookeeper 这样的框架高度依赖线程上下文类加载器 (TCCL) 来寻找自身扩展。即便在跨界调用时加入了 `Thread.currentThread().setContextClassLoader(...)` 上下文拦截包裹，一旦框架在内部新开了 `ThreadPool` 异步线程池，线程级上下文会瞬间断裂，导致 SPI 扩展寻址失败，甚至造成内存严重泄漏 (OOM)。

### 推荐方案原理：Maven Shade 包重定位 (Relocation)

区别于高风险的 ClassLoader 隔离魔改，在遇到重型框架环境干涉时，OpenCGL 官方强烈推荐使用 **Maven Shade Plugin (包阴影/重定位技术)**。

**原理**：在插件打包 JAR 的生命周期中，利用字节码工程 (ASM) 技术，将容易冲突的第三方库源码包名**强行重命名**为一个只属于当前插件自己的独特包名（如将 `org.apache.zookeeper` 整体变更为 `com.myplugin.shaded.apache.zookeeper`）。
这一技术直接从物理代码面上抹除了冲突基因，从 JVM 视角看这就是另一个独立类。无论外部环境怎样恶劣，它始终纯净自居并依然能安稳行驶在最稳定的原生双亲委派加载器机制上。

### 集成方式

在发生冲突的特殊插件的 `pom.xml` 中，追加这段配置：

```xml
<build>
    <plugins>
        <!-- 引入 maven-shade-plugin -->
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-shade-plugin</artifactId>
            <version>3.5.0</version>
            <executions>
                <execution>
                    <phase>package</phase> <!-- 绑定到打包阶段 -->
                    <goals>
                        <goal>shade</goal>
                    </goals>
                    <configuration>
                        <!-- (可选) 生成干练不带原始声明的 pom 文件 -->
                        <createDependencyReducedPom>false</createDependencyReducedPom>
                        
                        <relocations>
                            <!-- 1. 将冲突的原生包转移至插件特有私有域中 -->
                            <relocation>
                                <pattern>org.apache.zookeeper</pattern>
                                <!-- 新包名前缀加一个 shaded 作为标志，必须唯一不重叠！ -->
                                <shadedPattern>com.myplugin.shaded.apache.zookeeper</shadedPattern>
                            </relocation>
                            <!-- 可以并列多个，如经常作祟的 netty 或 dubbo 关联底层库 -->
                            <relocation>
                                <pattern>io.netty</pattern>
                                <shadedPattern>com.myplugin.shaded.io.netty</shadedPattern>
                            </relocation>
                        </relocations>
                        
                        <!-- 2. (非常关键)合并 SPI 文件描述：Zookeeper或Dubbo大量运用了SPI，
                             如果不加这个标签，包名虽改但 META-INF/services/ 下的寻址文本依然是老包名就会宕机。
                             该指令会连同文本以及文件名字一并处理安全映射转换。 -->
                        <transformers>
                            <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                        </transformers>
                    </configuration>
                </execution>
            </executions>
        </plugin>
    </plugins>
</build>
```

### 方案对比评估

为了直观展示其含金量，这里做一横向参照对比：

| 隔离方案 | 实现难度 | 运行时性能开销 | 稳定性/踩坑可能 | 建议适用场景 |
| :------- | :------- | :------------- | :-------------- | :------- |
| **原生双亲委派 (当前)** | 零配置 | 极低极快 (原生指针) | 低 (偶发环境包名冲突) | 95% 以上的标准、轻量级功能插件 |
| **自定义 Child-First** | 极高 (白名单机制+TCCL动态绑核) | 中等偏低 (线程切换与锁) | **极高** (SPI穿透失效、线程池错乱、永久泄漏) | 极端的全局复杂后端引擎中间件、或应用服务器底座 |
| **Shade 静态包重定位** | 极低 (复制粘贴一次 Maven 插件) | 极低极快 (原生安全零包裹机制) | 极低 (无懈可击的最深层次物理包名隔离) | **存在包含 Dubbo/ElasticSearch/Netty 的极重型孤岛框架** |

> **总结**：Shade 带来唯一几乎可忽略的副作用，仅仅是生成的 Plugin JAR 体积可能会大出几兆（包含了 Shade 进来的自用依赖）。但它不仅在隔离性上做到了降维打击，而且彻底解除了你在写底层多线程加载器调度时的所有心理负担与未知隐患。


## 构建与调试

### 1. 插件父 POM 与 Maven Profile（公共包由 parent 统一控制）

插件以 **parent** 引用 `com.opencgl.plugin`，公共依赖（含 OpenCGL-Base）由该父 POM 的 **profile** 统一控制，各插件模块无需重复声明 Base 版本与 scope。

| Profile | 说明 | OpenCGL-Base 与公共组件 |
| :--- | :--- | :--- |
| **plugin-mode**（默认） | 父 POM 以 `provided` 注入 JavaFX、MaterialFX、OpenCGL-Base 等。 | 插件 JAR 不打包 Base；运行时由**主程序**提供，主题、语言、LoadingMask 等与主程序一致。 |
| **stand-alone-mode** | 父 POM 以可打包方式注入依赖，便于单独运行。 | 插件包内自带 Base，适合本地调试或独立分发。 |

构建时若不指定 profile，默认使用 `plugin-mode`；需要独立运行时可执行：  
`mvn clean package -Pstand-alone-mode`。

**与主程序的关系**：主程序（OpenCGL_New）单独依赖 `com.opencgl.base`，不继承 `com.opencgl.plugin`。主程序是 Base 的**唯一提供方**（编译并随主程序分发），插件在 plugin-mode 下使用同一份。建议主程序与插件父 POM 中 Base 的版本保持一致，或通过共享 BOM/父 POM 统一管理版本。

### 2. 主程序构建顺序

主程序仅依赖 **api**、**base**，传递依赖来自本地仓库中的 OpenCGL-Base。**首次构建或修改过 Base/API 后**，须先安装 Base（及 API，若在独立工程）再编译主程序：

1. 在 **OpenCGL-Base** 目录（如 `OpenCGL-Plugin-New/OpenCGL-Base`）执行：`mvn install -DskipTests`
2. 若 API 在独立模块，同样先在该处执行 `mvn install`
3. 再在**主程序**（`OpenCGL_New`）下执行：`mvn compile` 或 `mvn package`

详见《主程序与插件依赖改造方案》第 5 节「构建顺序说明」。

### 3. 自动部署
在 `pom.xml` 中配置 `maven-antrun-plugin`，在构建成功后自动将 jar 复制到 OpenCGL 的 `plugins` 或 `bin` 目录（例如 `../bin`）。

```xml
<plugin>
    <artifactId>maven-antrun-plugin</artifactId>
    <executions>
        <execution>
            <phase>package</phase>
            <goals><goal>run</goal></goals>
            <configuration>
                <target>
                    <copy file="${project.build.directory}/${project.build.finalName}.jar"
                          tofile="../bin/${project.build.finalName}.jar"/>
                </target>
            </configuration>
        </execution>
    </executions>
</plugin>
```

### 4. 调试
启动主程序 `OpenCGL_New`，带上 JVM 参数：
```bash
-Dopencgl.dev.mode=true
```
主程序启动后，会自动扫描同级目录或 `plugins` 目录加载你的插件 jar。你可以在 IDE 中 attach 到主程序进程进行调试。

---

## 最佳实践

1.  **UI 线程安全**：所有 UI 更新必须在 JavaFX 应用线程中执行。使用 `Platform.runLater(() -> { ... })`。
2.  **异步操作**：耗时操作（网络请求、IO）必须在后台线程执行，禁止阻塞 UI 线程。推荐使用 `CompletableFuture`。
3.  **异常处理**：做好异常捕获，避免插件内部错误导致主程序崩溃。
4.  **资源隔离**：尽量不要修改 `System.properties` 或全局静态变量，以免影响其他插件。
5.  **图标与分类**：`iconPath()` 返回资源 URL 即可，主程序会以流方式加载；`directoryName()` 可与主程序 `opencgl.category.*` 配置配合实现分类名多语言。
6.  **createView() 性能**：尽量保持轻量，避免在 FX 线程中做大量 I/O 或复杂计算，以便主程序 LoadingMask 动画流畅。
7.  **插件内 LoadingMask**：若使用 OpenCGL-Base 的 `LoadingMask`，请 `show` 在不会被整体替换的父节点上（如根布局），并在操作完成后及时 `hide()`，避免遮罩被意外移除或一直存在。